apiVersion: v1
kind: Service
metadata:
  name: {{ include "platform-engineering.fullname" . }}
  labels:
    {{- include "platform-engineering.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "platform-engineering.selectorLabels" . | nindent 4 }}
